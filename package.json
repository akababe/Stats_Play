{
  "name": "statplay",
  "version": "1.0.0",
  "description": "StatPlay Interactive Statistics Platform",
  "scripts": {
    "dev:engine":  "cd engine && python -m uvicorn main:app --reload --port 8000",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend":"cd frontend && npm run dev",
    "dev:all":     "concurrently --names 'ENGINE,BACKEND,FRONTEND' --prefix-colors 'blue,green,magenta' \"npm run dev:engine\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "install:python": "pip install -r engine/requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
